/**
 * Host apply for the cursor plugin. No host-side behaviour — the effect is
 * pure chrome over the composer textarea persisted in localStorage, so no
 * host settings registration is needed.
 */
export function apply(): void {}
